#!/usr/bin/env node
var path = require('path'),
    fs = require('fs'),
    lib = path.join(path.dirname(fs.realpathSync(__filename)), '../');

var home_dir = process.env.HOME;
require(lib + '/main.js');
require(lib + '/help.txt');